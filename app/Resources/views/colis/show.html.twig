{% extends 'base.html.twig' %}

{% block body %}
    <h1>Coli</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ coli.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ coli.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('colis_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('colis_edit', { 'id': coli.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
